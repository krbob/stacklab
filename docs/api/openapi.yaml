openapi: 3.1.0
info:
  title: Stacklab API
  version: 0.1.0
  summary: Compose-first homelab control panel API
servers:
  - url: /api
security:
  - sessionCookie: []
tags:
  - name: Health
  - name: Auth
  - name: Meta
  - name: Host
  - name: Docker
  - name: Config
  - name: Git
  - name: Maintenance
  - name: Stacks
  - name: Audit
  - name: Jobs
  - name: Settings
paths:
  /health:
    get:
      tags: [Health]
      operationId: getHealth
      security: []
      responses:
        '200':
          description: Service health status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
  /session:
    get:
      tags: [Auth]
      operationId: getSession
      responses:
        '200':
          description: Current session status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
  /meta:
    get:
      tags: [Meta]
      operationId: getMeta
      responses:
        '200':
          description: Application metadata.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MetaResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
  /host/overview:
    get:
      tags: [Host]
      operationId: getHostOverview
      responses:
        '200':
          description: Host and Stacklab runtime metadata.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HostOverviewResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
  /host/stacklab-logs:
    get:
      tags: [Host]
      operationId: getStacklabLogs
      parameters:
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 1000
            default: 200
        - in: query
          name: cursor
          required: false
          schema:
            type: string
        - in: query
          name: level
          required: false
          schema:
            type: string
            enum: [debug, info, warn, error]
        - in: query
          name: q
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Recent Stacklab service logs from journald.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StacklabLogsResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
        '400':
          $ref: '#/components/responses/ErrorValidationFailed'
  /docker/admin/overview:
    get:
      tags: [Docker]
      operationId: getDockerAdminOverview
      responses:
        '200':
          description: Docker daemon administration overview.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DockerAdminOverviewResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
  /docker/admin/daemon-config:
    get:
      tags: [Docker]
      operationId: getDockerAdminDaemonConfig
      responses:
        '200':
          description: Read-only Docker daemon config file view.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DockerDaemonConfigResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
  /config/workspace/tree:
    get:
      tags: [Config]
      operationId: getConfigWorkspaceTree
      parameters:
        - in: query
          name: path
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Directory-scoped config workspace listing.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfigTreeResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
        '400':
          description: Invalid path or path is not a directory.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorNotFound'
  /config/workspace/file:
    get:
      tags: [Config]
      operationId: getConfigWorkspaceFile
      parameters:
        - in: query
          name: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Config workspace file metadata and optional content.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfigFileResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
        '400':
          description: Invalid path or path is not a file.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorNotFound'
    put:
      tags: [Config]
      operationId: saveConfigWorkspaceFile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfigFileSaveRequest'
      responses:
        '200':
          description: Config workspace file saved.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfigFileSaveResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
        '400':
          description: Invalid path or parent directory shape.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorNotFound'
        '409':
          description: Binary files are not editable in-browser.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /git/workspace/status:
    get:
      tags: [Git]
      operationId: getGitWorkspaceStatus
      responses:
        '200':
          description: Read-only Git status for the managed Stacklab workspace.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GitWorkspaceStatusResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
  /git/workspace/diff:
    get:
      tags: [Git]
      operationId: getGitWorkspaceDiff
      parameters:
        - in: query
          name: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Unified diff for one changed managed file.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GitDiffResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
        '400':
          description: Invalid path or path outside managed roots.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorNotFound'
        '503':
          description: Git workspace is unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /git/workspace/commit:
    post:
      tags: [Git]
      operationId: commitGitWorkspace
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GitCommitRequest'
      responses:
        '200':
          description: Local commit created for selected managed files.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GitCommitResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
        '400':
          description: Invalid commit request or invalid path selection.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          $ref: '#/components/responses/ErrorNotFound'
        '409':
          description: Commit cannot proceed due to conflicts, permissions, or no staged changes.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          description: Git workspace is unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /git/workspace/push:
    post:
      tags: [Git]
      operationId: pushGitWorkspace
      responses:
        '200':
          description: Current branch push result for configured upstream.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GitPushResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
        '409':
          description: Upstream is missing or remote rejected the push.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Remote authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          description: Git workspace is unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /maintenance/update-stacks:
    post:
      tags: [Maintenance]
      operationId: updateStacks
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MaintenanceUpdateStacksRequest'
      responses:
        '200':
          description: Maintenance update job accepted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobEnvelopeResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
        '404':
          $ref: '#/components/responses/ErrorNotFound'
        '409':
          description: One of the selected stacks is locked or invalid for update.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /maintenance/images:
    get:
      tags: [Maintenance]
      operationId: getMaintenanceImages
      parameters:
        - in: query
          name: q
          required: false
          schema:
            type: string
        - in: query
          name: usage
          required: false
          schema:
            type: string
            enum: [all, used, unused]
        - in: query
          name: origin
          required: false
          schema:
            type: string
            enum: [all, stack_managed, external]
      responses:
        '200':
          description: Maintenance image inventory.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MaintenanceImagesResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
        '400':
          $ref: '#/components/responses/ErrorValidationFailed'
        '503':
          description: Docker maintenance inventory is unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /maintenance/prune-preview:
    get:
      tags: [Maintenance]
      operationId: getMaintenancePrunePreview
      parameters:
        - in: query
          name: images
          required: false
          schema:
            type: boolean
        - in: query
          name: build_cache
          required: false
          schema:
            type: boolean
        - in: query
          name: stopped_containers
          required: false
          schema:
            type: boolean
        - in: query
          name: volumes
          required: false
          schema:
            type: boolean
      responses:
        '200':
          description: Prune preview summary.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MaintenancePrunePreviewResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
        '503':
          description: Docker prune preview is unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /maintenance/prune:
    post:
      tags: [Maintenance]
      operationId: runMaintenancePrune
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MaintenancePruneRequest'
      responses:
        '200':
          description: Prune job accepted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobEnvelopeResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
        '400':
          $ref: '#/components/responses/ErrorValidationFailed'
        '409':
          description: Another global or stack maintenance job is already running.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /auth/login:
    post:
      tags: [Auth]
      operationId: login
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Login succeeded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
  /auth/logout:
    post:
      tags: [Auth]
      operationId: logout
      responses:
        '200':
          description: Logout succeeded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LogoutResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
  /stacks:
    get:
      tags: [Stacks]
      operationId: listStacks
      parameters:
        - in: query
          name: q
          required: false
          schema:
            type: string
        - in: query
          name: sort
          required: false
          schema:
            type: string
            enum: [name, state, last_action]
      responses:
        '200':
          description: Stack list and summary.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StackListResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
    post:
      tags: [Stacks]
      operationId: createStack
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateStackRequest'
      responses:
        '200':
          description: Stack creation job accepted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobEnvelopeResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
        '409':
          $ref: '#/components/responses/ErrorConflict'
        '422':
          $ref: '#/components/responses/ErrorValidationFailed'
  /stacks/{stackId}:
    parameters:
      - $ref: '#/components/parameters/StackId'
    get:
      tags: [Stacks]
      operationId: getStack
      responses:
        '200':
          description: Detailed stack view model.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StackDetailResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
        '404':
          $ref: '#/components/responses/ErrorNotFound'
    delete:
      tags: [Stacks]
      operationId: deleteStack
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeleteStackRequest'
      responses:
        '200':
          description: Stack deletion job accepted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobEnvelopeResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
        '404':
          $ref: '#/components/responses/ErrorNotFound'
  /stacks/{stackId}/definition:
    parameters:
      - $ref: '#/components/parameters/StackId'
    get:
      tags: [Stacks]
      operationId: getStackDefinition
      responses:
        '200':
          description: Editable stack definition files.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StackDefinitionResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
        '404':
          $ref: '#/components/responses/ErrorNotFound'
        '409':
          $ref: '#/components/responses/ErrorInvalidState'
    put:
      tags: [Stacks]
      operationId: updateStackDefinition
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateDefinitionRequest'
      responses:
        '200':
          description: Definition save job accepted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobEnvelopeResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
        '404':
          $ref: '#/components/responses/ErrorNotFound'
        '409':
          $ref: '#/components/responses/ErrorInvalidState'
  /stacks/{stackId}/resolved-config:
    parameters:
      - $ref: '#/components/parameters/StackId'
    get:
      tags: [Stacks]
      operationId: getResolvedConfig
      parameters:
        - in: query
          name: source
          required: false
          schema:
            type: string
            enum: [current, last_valid]
      responses:
        '200':
          description: Resolved config or validation failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResolvedConfigResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
        '404':
          $ref: '#/components/responses/ErrorNotFound'
    post:
      tags: [Stacks]
      operationId: previewResolvedConfig
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResolvedConfigPreviewRequest'
      responses:
        '200':
          description: Draft resolved config preview or validation failure.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResolvedConfigResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
        '404':
          $ref: '#/components/responses/ErrorNotFound'
        '409':
          $ref: '#/components/responses/ErrorInvalidState'
  /stacks/{stackId}/audit:
    parameters:
      - $ref: '#/components/parameters/StackId'
    get:
      tags: [Audit]
      operationId: listStackAudit
      parameters:
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/Limit'
      responses:
        '200':
          description: Per-stack audit log.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditListResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
        '404':
          $ref: '#/components/responses/ErrorNotFound'
  /stacks/{stackId}/actions/{action}:
    parameters:
      - $ref: '#/components/parameters/StackId'
      - $ref: '#/components/parameters/StackAction'
    post:
      tags: [Stacks]
      operationId: runStackAction
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyObject'
      responses:
        '200':
          description: Stack action job accepted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobEnvelopeResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
        '404':
          $ref: '#/components/responses/ErrorNotFound'
        '409':
          description: Stack locked or invalid state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /audit:
    get:
      tags: [Audit]
      operationId: listAudit
      parameters:
        - in: query
          name: stack_id
          required: false
          schema:
            type: string
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/Limit'
      responses:
        '200':
          description: Global audit log.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditListResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
  /settings/password:
    post:
      tags: [Settings]
      operationId: updatePassword
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePasswordRequest'
      responses:
        '200':
          description: Password updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdatePasswordResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
  /jobs/active:
    get:
      tags: [Jobs]
      operationId: listActiveJobs
      responses:
        '200':
          description: Active jobs for global activity chrome.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActiveJobsResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
  /jobs/{jobId}:
    parameters:
      - $ref: '#/components/parameters/JobId'
    get:
      tags: [Jobs]
      operationId: getJob
      responses:
        '200':
          description: Job snapshot.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobDetailResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
        '404':
          $ref: '#/components/responses/ErrorNotFound'
  /jobs/{jobId}/cancel:
    parameters:
      - $ref: '#/components/parameters/JobId'
    post:
      tags: [Jobs]
      operationId: cancelJob
      responses:
        '200':
          description: Job cancellation requested.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobEnvelopeResponse'
        '401':
          $ref: '#/components/responses/ErrorUnauthorized'
        '404':
          $ref: '#/components/responses/ErrorNotFound'
        '409':
          $ref: '#/components/responses/ErrorInvalidState'
components:
  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: stacklab_session
  parameters:
    StackId:
      in: path
      name: stackId
      required: true
      schema:
        type: string
        pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'
    JobId:
      in: path
      name: jobId
      required: true
      schema:
        type: string
    Cursor:
      in: query
      name: cursor
      required: false
      schema:
        type: string
    Limit:
      in: query
      name: limit
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 50
    StackAction:
      in: path
      name: action
      required: true
      schema:
        type: string
        enum: [validate, up, down, stop, restart, pull, build, recreate]
  responses:
    ErrorUnauthorized:
      description: Authentication required or invalid session.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    ErrorNotFound:
      description: Resource not found.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    ErrorConflict:
      description: Request conflicts with current state.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    ErrorInvalidState:
      description: Operation not allowed in current state.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    ErrorValidationFailed:
      description: Validation failed.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
  schemas:
    EmptyObject:
      type: object
      additionalProperties: false
    HealthResponse:
      type: object
      properties:
        status:
          type: string
          const: ok
        version:
          type: string
      required: [status, version]
    SessionResponse:
      type: object
      properties:
        authenticated:
          type: boolean
        user:
          $ref: '#/components/schemas/SessionUser'
        features:
          $ref: '#/components/schemas/FeatureFlags'
      required: [authenticated, features]
    SessionUser:
      type: object
      properties:
        id:
          type: string
        display_name:
          type: string
      required: [id, display_name]
    FeatureFlags:
      type: object
      properties:
        host_shell:
          type: boolean
      required: [host_shell]
    MetaResponse:
      type: object
      properties:
        app:
          type: object
          properties:
            name:
              type: string
            version:
              type: string
          required: [name, version]
        environment:
          type: object
          properties:
            stack_root:
              type: string
            platform:
              type: string
          required: [stack_root, platform]
        docker:
          type: object
          properties:
            engine_version:
              type: string
            compose_version:
              type: string
          required: [engine_version, compose_version]
        features:
          $ref: '#/components/schemas/FeatureFlags'
      required: [app, environment, docker, features]
    HostOverviewResponse:
      type: object
      properties:
        host:
          $ref: '#/components/schemas/HostMeta'
        stacklab:
          $ref: '#/components/schemas/StacklabMeta'
        docker:
          $ref: '#/components/schemas/DockerMeta'
        resources:
          $ref: '#/components/schemas/HostResources'
      required: [host, stacklab, docker, resources]
    HostMeta:
      type: object
      properties:
        hostname:
          type: string
        os_name:
          type: string
        kernel_version:
          type: string
        architecture:
          type: string
        uptime_seconds:
          type: integer
      required: [hostname, os_name, kernel_version, architecture, uptime_seconds]
    StacklabMeta:
      type: object
      properties:
        version:
          type: string
        commit:
          type: string
        started_at:
          type: string
          format: date-time
      required: [version, commit, started_at]
    DockerMeta:
      type: object
      properties:
        engine_version:
          type: string
        compose_version:
          type: string
      required: [engine_version, compose_version]
    HostResources:
      type: object
      properties:
        cpu:
          $ref: '#/components/schemas/HostCPU'
        memory:
          $ref: '#/components/schemas/HostMemory'
        disk:
          $ref: '#/components/schemas/HostDisk'
      required: [cpu, memory, disk]
    HostCPU:
      type: object
      properties:
        core_count:
          type: integer
        load_average:
          type: array
          items:
            type: number
            format: double
        usage_percent:
          type: number
          format: double
      required: [core_count, load_average, usage_percent]
    HostMemory:
      type: object
      properties:
        total_bytes:
          type: integer
        used_bytes:
          type: integer
        available_bytes:
          type: integer
        usage_percent:
          type: number
          format: double
      required: [total_bytes, used_bytes, available_bytes, usage_percent]
    HostDisk:
      type: object
      properties:
        path:
          type: string
        total_bytes:
          type: integer
        used_bytes:
          type: integer
        available_bytes:
          type: integer
        usage_percent:
          type: number
          format: double
      required: [path, total_bytes, used_bytes, available_bytes, usage_percent]
    StacklabLogsResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/StacklabLogEntry'
        next_cursor:
          type: string
          nullable: true
        has_more:
          type: boolean
      required: [items, has_more]
    StacklabLogEntry:
      type: object
      properties:
        timestamp:
          type: string
          format: date-time
        level:
          type: string
          enum: [debug, info, warn, error]
        message:
          type: string
        cursor:
          type: string
      required: [timestamp, level, message, cursor]
    DockerAdminOverviewResponse:
      type: object
      properties:
        service:
          $ref: '#/components/schemas/DockerServiceStatus'
        engine:
          $ref: '#/components/schemas/DockerEngineStatus'
        daemon_config:
          $ref: '#/components/schemas/DockerDaemonConfigMeta'
      required: [service, engine, daemon_config]
    DockerServiceStatus:
      type: object
      properties:
        manager:
          type: string
        supported:
          type: boolean
        unit_name:
          type: string
        load_state:
          type: string
        active_state:
          type: string
        sub_state:
          type: string
        unit_file_state:
          type: string
        fragment_path:
          type: string
        started_at:
          type: string
          format: date-time
          nullable: true
        message:
          type: string
          nullable: true
      required: [manager, supported, unit_name, load_state, active_state, sub_state, unit_file_state, fragment_path]
    DockerEngineStatus:
      type: object
      properties:
        available:
          type: boolean
        version:
          type: string
        api_version:
          type: string
        compose_version:
          type: string
        root_dir:
          type: string
        driver:
          type: string
        logging_driver:
          type: string
        cgroup_driver:
          type: string
        message:
          type: string
          nullable: true
      required: [available, version, api_version, compose_version, root_dir, driver, logging_driver, cgroup_driver]
    DockerDaemonConfigSummary:
      type: object
      properties:
        dns:
          type: array
          items:
            type: string
        registry_mirrors:
          type: array
          items:
            type: string
        insecure_registries:
          type: array
          items:
            type: string
        log_driver:
          type: string
        data_root:
          type: string
        live_restore:
          type: boolean
          nullable: true
      required: [dns, registry_mirrors, insecure_registries, log_driver, data_root]
    DockerDaemonConfigMeta:
      type: object
      properties:
        path:
          type: string
        exists:
          type: boolean
        permissions:
          $ref: '#/components/schemas/FilePermissionsNullable'
        size_bytes:
          type: integer
          nullable: true
        modified_at:
          type: string
          format: date-time
          nullable: true
        valid_json:
          type: boolean
        parse_error:
          type: string
          nullable: true
        configured_keys:
          type: array
          items:
            type: string
        summary:
          $ref: '#/components/schemas/DockerDaemonConfigSummary'
      required: [path, exists, valid_json, configured_keys, summary]
    DockerDaemonConfigResponse:
      allOf:
        - $ref: '#/components/schemas/DockerDaemonConfigMeta'
        - type: object
          properties:
            content:
              type: string
              nullable: true
    ConfigEntryType:
      type: string
      enum: [directory, text_file, binary_file, unknown_file]
    ConfigTreeResponse:
      type: object
      properties:
        workspace_root:
          type: string
        current_path:
          type: string
        parent_path:
          type: string
          nullable: true
        items:
          type: array
          items:
            $ref: '#/components/schemas/ConfigTreeEntry'
      required: [workspace_root, current_path, items]
    ConfigTreeEntry:
      type: object
      properties:
        name:
          type: string
        path:
          type: string
        type:
          $ref: '#/components/schemas/ConfigEntryType'
        size_bytes:
          type: integer
        modified_at:
          type: string
          format: date-time
        stack_id:
          type: string
          nullable: true
        permissions:
          $ref: '#/components/schemas/FilePermissions'
      required: [name, path, type, size_bytes, modified_at, permissions]
    ConfigFileResponse:
      type: object
      properties:
        path:
          type: string
        name:
          type: string
        type:
          $ref: '#/components/schemas/ConfigEntryType'
        stack_id:
          type: string
          nullable: true
        content:
          type: string
          nullable: true
        encoding:
          type: string
          nullable: true
        size_bytes:
          type: integer
        modified_at:
          type: string
          format: date-time
        readable:
          type: boolean
        writable:
          type: boolean
        blocked_reason:
          type: string
          nullable: true
        permissions:
          $ref: '#/components/schemas/FilePermissions'
      required: [path, name, type, size_bytes, modified_at, readable, writable, permissions]
    ConfigFileSaveRequest:
      type: object
      properties:
        path:
          type: string
        content:
          type: string
        create_parent_directories:
          type: boolean
      required: [path, content, create_parent_directories]
    ConfigFileSaveResponse:
      type: object
      properties:
        saved:
          type: boolean
        path:
          type: string
        modified_at:
          type: string
          format: date-time
        audit_action:
          type: string
      required: [saved, path, modified_at, audit_action]
    GitFileStatus:
      type: string
      enum: [modified, added, deleted, renamed, untracked, conflicted]
    GitScope:
      type: string
      enum: [stacks, config]
    GitWorkspaceStatusResponse:
      type: object
      properties:
        available:
          type: boolean
        repo_root:
          type: string
        managed_roots:
          type: array
          items:
            type: string
        branch:
          type: string
        head_commit:
          type: string
        has_upstream:
          type: boolean
        upstream_name:
          type: string
        ahead_count:
          type: integer
        behind_count:
          type: integer
        clean:
          type: boolean
        reason:
          type: string
        items:
          type: array
          items:
            $ref: '#/components/schemas/GitStatusItem'
      required: [available, repo_root, managed_roots]
    GitStatusItem:
      type: object
      properties:
        path:
          type: string
        scope:
          $ref: '#/components/schemas/GitScope'
        stack_id:
          type: string
          nullable: true
        status:
          $ref: '#/components/schemas/GitFileStatus'
        old_path:
          type: string
          nullable: true
        permissions:
          $ref: '#/components/schemas/FilePermissionsNullable'
        diff_available:
          type: boolean
        commit_allowed:
          type: boolean
        blocked_reason:
          type: string
          nullable: true
      required: [path, scope, status, diff_available, commit_allowed]
    GitDiffResponse:
      type: object
      properties:
        available:
          type: boolean
        path:
          type: string
        scope:
          $ref: '#/components/schemas/GitScope'
        stack_id:
          type: string
          nullable: true
        status:
          $ref: '#/components/schemas/GitFileStatus'
        old_path:
          type: string
          nullable: true
        permissions:
          $ref: '#/components/schemas/FilePermissionsNullable'
        diff_available:
          type: boolean
        blocked_reason:
          type: string
          nullable: true
        is_binary:
          type: boolean
        diff:
          type: string
          nullable: true
        truncated:
          type: boolean
      required: [available, path, scope, status, diff_available, is_binary, truncated]
    FilePermissions:
      type: object
      properties:
        owner_uid:
          type: integer
          nullable: true
        owner_name:
          type: string
          nullable: true
        group_gid:
          type: integer
          nullable: true
        group_name:
          type: string
          nullable: true
        mode:
          type: string
        readable:
          type: boolean
        writable:
          type: boolean
      required: [mode, readable, writable]
    FilePermissionsNullable:
      anyOf:
        - $ref: '#/components/schemas/FilePermissions'
        - type: 'null'
    GitCommitRequest:
      type: object
      properties:
        message:
          type: string
        paths:
          type: array
          items:
            type: string
      required: [message, paths]
    GitCommitResponse:
      type: object
      properties:
        committed:
          type: boolean
        commit:
          type: string
        summary:
          type: string
        paths:
          type: array
          items:
            type: string
        remaining_changes:
          type: integer
      required: [committed, commit, summary, paths, remaining_changes]
    GitPushResponse:
      type: object
      properties:
        pushed:
          type: boolean
        remote:
          type: string
        branch:
          type: string
        upstream_name:
          type: string
        head_commit:
          type: string
        ahead_count:
          type: integer
        behind_count:
          type: integer
      required: [pushed, remote, branch, upstream_name, head_commit, ahead_count, behind_count]
    StackListResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/StackListItem'
        summary:
          $ref: '#/components/schemas/StackListSummary'
      required: [items, summary]
    StackListItem:
      allOf:
        - $ref: '#/components/schemas/StackHeader'
        - type: object
          properties:
            service_count:
              type: object
              properties:
                defined:
                  type: integer
                running:
                  type: integer
              required: [defined, running]
            last_action:
              $ref: '#/components/schemas/LastAction'
          required: [service_count]
    StackListSummary:
      type: object
      properties:
        stack_count:
          type: integer
        running_count:
          type: integer
        stopped_count:
          type: integer
        error_count:
          type: integer
        defined_count:
          type: integer
        orphaned_count:
          type: integer
        container_count:
          type: object
          properties:
            running:
              type: integer
            total:
              type: integer
          required: [running, total]
      required:
        - stack_count
        - running_count
        - stopped_count
        - error_count
        - defined_count
        - orphaned_count
        - container_count
    StackDetailResponse:
      type: object
      properties:
        stack:
          $ref: '#/components/schemas/StackDetail'
      required: [stack]
    StackDetail:
      allOf:
        - $ref: '#/components/schemas/StackHeader'
        - type: object
          properties:
            root_path:
              type: string
            compose_file_path:
              type: string
            env_file_path:
              type: string
            config_path:
              type: string
            data_path:
              type: string
            capabilities:
              $ref: '#/components/schemas/StackCapabilities'
            available_actions:
              type: array
              items:
                type: string
            services:
              type: array
              items:
                $ref: '#/components/schemas/Service'
            containers:
              type: array
              items:
                $ref: '#/components/schemas/Container'
            last_deployed_at:
              type: string
              format: date-time
              nullable: true
            last_action:
              $ref: '#/components/schemas/LastAction'
          required:
            - root_path
            - compose_file_path
            - config_path
            - data_path
            - capabilities
            - available_actions
            - services
            - containers
    StackHeader:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        display_state:
          $ref: '#/components/schemas/DisplayState'
        runtime_state:
          $ref: '#/components/schemas/RuntimeState'
        config_state:
          $ref: '#/components/schemas/ConfigState'
        activity_state:
          $ref: '#/components/schemas/ActivityState'
        health_summary:
          $ref: '#/components/schemas/HealthSummary'
      required:
        - id
        - name
        - created_at
        - updated_at
        - display_state
        - runtime_state
        - config_state
        - activity_state
        - health_summary
    HealthSummary:
      type: object
      properties:
        healthy_container_count:
          type: integer
        unhealthy_container_count:
          type: integer
        unknown_health_container_count:
          type: integer
      required:
        - healthy_container_count
        - unhealthy_container_count
        - unknown_health_container_count
    StackCapabilities:
      type: object
      properties:
        can_edit_definition:
          type: boolean
        can_view_logs:
          type: boolean
        can_view_stats:
          type: boolean
        can_open_terminal:
          type: boolean
      required:
        - can_edit_definition
        - can_view_logs
        - can_view_stats
        - can_open_terminal
    Service:
      type: object
      properties:
        name:
          type: string
        mode:
          $ref: '#/components/schemas/ServiceMode'
        image_ref:
          type: string
          nullable: true
        build_context:
          type: string
          nullable: true
        dockerfile_path:
          type: string
          nullable: true
        ports:
          type: array
          items:
            $ref: '#/components/schemas/PortMapping'
        volumes:
          type: array
          items:
            $ref: '#/components/schemas/VolumeMount'
        depends_on:
          type: array
          items:
            type: string
        healthcheck_present:
          type: boolean
      required:
        - name
        - mode
        - ports
        - volumes
        - depends_on
        - healthcheck_present
    Container:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        service_name:
          type: string
        status:
          $ref: '#/components/schemas/ContainerStatus'
        health_status:
          type: string
          nullable: true
        started_at:
          type: string
          format: date-time
          nullable: true
        image_id:
          type: string
        image_ref:
          type: string
        ports:
          type: array
          items:
            $ref: '#/components/schemas/PortMapping'
        networks:
          type: array
          items:
            type: string
      required:
        - id
        - name
        - service_name
        - status
        - image_id
        - image_ref
        - ports
        - networks
    PortMapping:
      type: object
      properties:
        published:
          type: integer
        target:
          type: integer
        protocol:
          type: string
      required: [published, target, protocol]
    VolumeMount:
      type: object
      properties:
        source:
          type: string
        target:
          type: string
      required: [source, target]
    LastAction:
      type: object
      properties:
        action:
          type: string
        result:
          $ref: '#/components/schemas/JobStateTerminal'
        finished_at:
          type: string
          format: date-time
      required: [action, result, finished_at]
      nullable: true
    StackDefinitionResponse:
      type: object
      properties:
        stack_id:
          type: string
        files:
          type: object
          properties:
            compose_yaml:
              type: object
              properties:
                path:
                  type: string
                content:
                  type: string
              required: [path, content]
            env:
              type: object
              properties:
                path:
                  type: string
                content:
                  type: string
                exists:
                  type: boolean
              required: [path, content, exists]
          required: [compose_yaml, env]
        config_state:
          $ref: '#/components/schemas/ConfigState'
      required: [stack_id, files, config_state]
    ResolvedConfigPreviewRequest:
      type: object
      properties:
        compose_yaml:
          type: string
        env:
          type: string
      required: [compose_yaml, env]
    ResolvedConfigResponse:
      oneOf:
        - type: object
          properties:
            stack_id:
              type: string
            valid:
              type: boolean
              const: true
            content:
              type: string
          required: [stack_id, valid, content]
        - type: object
          properties:
            stack_id:
              type: string
            valid:
              type: boolean
              const: false
            error:
              $ref: '#/components/schemas/ErrorDetail'
          required: [stack_id, valid, error]
    AuditListResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AuditEntry'
        next_cursor:
          type: string
          nullable: true
      required: [items]
    AuditEntry:
      type: object
      properties:
        id:
          type: string
        stack_id:
          type: string
          nullable: true
        job_id:
          type: string
          nullable: true
        action:
          type: string
        requested_by:
          type: string
        result:
          type: string
        requested_at:
          type: string
          format: date-time
        finished_at:
          type: string
          format: date-time
          nullable: true
        duration_ms:
          type: integer
          nullable: true
      required:
        - id
        - action
        - requested_by
        - result
        - requested_at
    LoginRequest:
      type: object
      properties:
        password:
          type: string
      required: [password]
    LoginResponse:
      type: object
      properties:
        authenticated:
          type: boolean
          const: true
      required: [authenticated]
    LogoutResponse:
      type: object
      properties:
        authenticated:
          type: boolean
          const: false
      required: [authenticated]
    CreateStackRequest:
      type: object
      properties:
        stack_id:
          type: string
          pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'
        compose_yaml:
          type: string
        env:
          type: string
        create_config_dir:
          type: boolean
        create_data_dir:
          type: boolean
        deploy_after_create:
          type: boolean
      required:
        - stack_id
        - compose_yaml
        - env
        - create_config_dir
        - create_data_dir
        - deploy_after_create
    UpdateDefinitionRequest:
      type: object
      properties:
        compose_yaml:
          type: string
        env:
          type: string
        validate_after_save:
          type: boolean
      required: [compose_yaml, env, validate_after_save]
    DeleteStackRequest:
      type: object
      properties:
        remove_runtime:
          type: boolean
        remove_definition:
          type: boolean
        remove_config:
          type: boolean
        remove_data:
          type: boolean
      required:
        - remove_runtime
        - remove_definition
        - remove_config
        - remove_data
    UpdatePasswordRequest:
      type: object
      properties:
        current_password:
          type: string
        new_password:
          type: string
      required: [current_password, new_password]
    UpdatePasswordResponse:
      type: object
      properties:
        updated:
          type: boolean
      required: [updated]
    JobEnvelopeResponse:
      type: object
      properties:
        job:
          $ref: '#/components/schemas/Job'
      required: [job]
    JobDetailResponse:
      type: object
      properties:
        job:
          $ref: '#/components/schemas/Job'
      required: [job]
    ActiveJobsResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ActiveJobItem'
        summary:
          $ref: '#/components/schemas/ActiveJobSummary'
      required: [items, summary]
    Job:
      type: object
      properties:
        id:
          type: string
        stack_id:
          type: string
          nullable: true
        action:
          type: string
        state:
          $ref: '#/components/schemas/JobState'
        requested_at:
          type: string
          format: date-time
        started_at:
          type: string
          format: date-time
          nullable: true
        finished_at:
          type: string
          format: date-time
          nullable: true
        workflow:
          type: object
          properties:
            steps:
              type: array
              items:
                type: object
                properties:
                  action:
                    type: string
                  state:
                    $ref: '#/components/schemas/JobState'
                  target_stack_id:
                    type: string
                required: [action, state]
          required: [steps]
          nullable: true
      required:
        - id
        - stack_id
        - action
        - state
    ActiveJobItem:
      type: object
      properties:
        id:
          type: string
        stack_id:
          type: string
          nullable: true
        action:
          type: string
        state:
          $ref: '#/components/schemas/JobState'
        requested_at:
          type: string
          format: date-time
        started_at:
          type: string
          format: date-time
          nullable: true
        workflow:
          $ref: '#/components/schemas/ActiveJobWorkflow'
        current_step:
          $ref: '#/components/schemas/ActiveJobStep'
        latest_event:
          $ref: '#/components/schemas/ActiveJobLatestEvent'
      required: [id, stack_id, action, state, requested_at]
    ActiveJobWorkflow:
      type: object
      properties:
        steps:
          type: array
          items:
            type: object
            properties:
              action:
                type: string
              state:
                $ref: '#/components/schemas/JobState'
              target_stack_id:
                type: string
            required: [action, state]
      required: [steps]
      nullable: true
    ActiveJobStep:
      type: object
      properties:
        index:
          type: integer
        total:
          type: integer
        action:
          type: string
        target_stack_id:
          type: string
      required: [index, total, action]
      nullable: true
    ActiveJobLatestEvent:
      type: object
      properties:
        event:
          type: string
        message:
          type: string
        data:
          type: string
          nullable: true
        timestamp:
          type: string
          format: date-time
        step:
          $ref: '#/components/schemas/ActiveJobStep'
      required: [event, timestamp]
      nullable: true
    ActiveJobSummary:
      type: object
      properties:
        active_count:
          type: integer
        running_count:
          type: integer
        queued_count:
          type: integer
        cancel_requested_count:
          type: integer
      required: [active_count, running_count, queued_count, cancel_requested_count]
    MaintenanceUpdateStacksRequest:
      type: object
      properties:
        target:
          type: object
          properties:
            mode:
              type: string
              enum: [selected, all]
            stack_ids:
              type: array
              items:
                type: string
          required: [mode]
        options:
          type: object
          properties:
            pull_images:
              type: boolean
              default: true
            build_images:
              type: boolean
              default: true
            remove_orphans:
              type: boolean
              default: true
            prune_after:
              type: object
              properties:
                enabled:
                  type: boolean
                  default: false
                include_volumes:
                  type: boolean
                  default: false
      required: [target]
    MaintenanceImagesResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/MaintenanceImageItem'
      required: [items]
    MaintenanceImageItem:
      type: object
      properties:
        id:
          type: string
        repository:
          type: string
        tag:
          type: string
        reference:
          type: string
        size_bytes:
          type: integer
        created_at:
          type: string
          format: date-time
        containers_using:
          type: integer
        stacks_using:
          type: array
          items:
            $ref: '#/components/schemas/MaintenanceImageStackUsage'
        is_dangling:
          type: boolean
        is_unused:
          type: boolean
        source:
          type: string
          enum: [stack_managed, external]
      required:
        - id
        - repository
        - tag
        - reference
        - size_bytes
        - created_at
        - containers_using
        - stacks_using
        - is_dangling
        - is_unused
        - source
    MaintenanceImageStackUsage:
      type: object
      properties:
        stack_id:
          type: string
        service_names:
          type: array
          items:
            type: string
      required: [stack_id, service_names]
    MaintenancePrunePreviewResponse:
      type: object
      properties:
        preview:
          $ref: '#/components/schemas/MaintenancePrunePreview'
      required: [preview]
    MaintenancePrunePreview:
      type: object
      properties:
        images:
          $ref: '#/components/schemas/MaintenancePrunePreviewCategory'
        build_cache:
          $ref: '#/components/schemas/MaintenancePrunePreviewCategory'
        stopped_containers:
          $ref: '#/components/schemas/MaintenancePrunePreviewCategory'
        volumes:
          $ref: '#/components/schemas/MaintenancePrunePreviewCategory'
        total_reclaimable_bytes:
          type: integer
      required:
        - images
        - build_cache
        - stopped_containers
        - volumes
        - total_reclaimable_bytes
    MaintenancePrunePreviewCategory:
      type: object
      properties:
        count:
          type: integer
        reclaimable_bytes:
          type: integer
        items:
          type: array
          items:
            $ref: '#/components/schemas/MaintenancePrunePreviewItem'
      required: [count, reclaimable_bytes]
    MaintenancePrunePreviewItem:
      type: object
      properties:
        reference:
          type: string
        size_bytes:
          type: integer
        reason:
          type: string
      required: [reference, size_bytes, reason]
    MaintenancePruneRequest:
      type: object
      properties:
        scope:
          type: object
          properties:
            images:
              type: boolean
            build_cache:
              type: boolean
            stopped_containers:
              type: boolean
            volumes:
              type: boolean
          required: [images, build_cache, stopped_containers, volumes]
      required: [scope]
    ErrorResponse:
      type: object
      properties:
        error:
          $ref: '#/components/schemas/ErrorDetail'
      required: [error]
    ErrorDetail:
      type: object
      properties:
        code:
          type: string
        message:
          type: string
        details:
          oneOf:
            - type: object
            - type: 'null'
      required: [code, message]
    RuntimeState:
      type: string
      enum: [defined, running, partial, stopped, error, orphaned]
    DisplayState:
      type: string
      enum: [defined, running, partial, stopped, error, orphaned]
    ConfigState:
      type: string
      enum: [unknown, in_sync, drifted, invalid]
    ActivityState:
      type: string
      enum: [idle, locked]
    ServiceMode:
      type: string
      enum: [image, build, hybrid]
    ContainerStatus:
      type: string
      enum: [created, running, restarting, paused, exited, dead]
    JobState:
      type: string
      enum: [queued, running, succeeded, failed, cancel_requested, cancelled, timed_out]
    JobStateTerminal:
      type: string
      enum: [succeeded, failed, cancelled, timed_out]
